# 任务1：一元线性回归的残差性质

一元线性回归模型为：

$$
y_i=\hat{\beta}_0+\hat{\beta}_1x_i+e_i
$$

其中残差：

$$
e_i=y_i-\hat{\beta}_0-\hat{\beta}_1x_i
$$

最小二乘法使残差平方和最小：

$$
RSS=\sum_{i=1}^{n}(y_i-\hat{\beta}_0-\hat{\beta}_1x_i)^2
$$

## 1. 证明残差之和等于0

对 $\hat{\beta}_0$ 求偏导，并令其等于0：

$$
\frac{\partial RSS}{\partial\hat{\beta}_0}
=-2\sum_{i=1}^{n}(y_i-\hat{\beta}_0-\hat{\beta}_1x_i)=0
$$

括号中的式子就是 $e_i$，因此：

$$
\boxed{\sum_{i=1}^{n}e_i=0}
$$

## 2. 证明残差与自变量乘积之和等于0

对 $\hat{\beta}_1$ 求偏导，并令其等于0：

$$
\frac{\partial RSS}{\partial\hat{\beta}_1}
=-2\sum_{i=1}^{n}x_i(y_i-\hat{\beta}_0-\hat{\beta}_1x_i)=0
$$

代入残差 $e_i$，得到：

$$
\boxed{\sum_{i=1}^{n}x_ie_i=0}
$$

## 结论

在含截距项的一元线性回归中，残差之和为0，并且残差与自变量正交。

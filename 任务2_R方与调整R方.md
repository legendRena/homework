# 任务2：$R^2$ 与调整 $R^2$

## 1. 为什么加入变量后 $R^2$ 不会下降

普通决定系数为：

$$
R^2=1-\frac{RSS}{TSS}
$$

加入一个新变量后，可以把该变量的系数设为0，因此新模型至少能保持原模型的拟合效果：

$$
RSS_{新}\leq RSS_{旧}
$$

由于 $TSS$ 不变，所以：

$$
R^2_{新}\geq R^2_{旧}
$$

因此，即使加入的是无关随机变量，$R^2$ 也只会上升或保持不变。

## 2. 为什么调整 $R^2$ 可能下降

若原模型有 $k$ 个自变量，则：

$$
R_{adj}^2=1-\frac{RSS/(n-k-1)}{TSS/(n-1)}
$$

加入一个变量后，残差自由度由 $n-k-1$ 减少为 $n-k-2$。调整 $R^2$ 下降的条件是：

$$
\frac{RSS_{新}}{n-k-2}>\frac{RSS_{旧}}{n-k-1}
$$

也就是说，如果新变量带来的 RSS 降幅太小，不足以弥补损失的自由度，调整 $R^2$ 就会下降。

## 结论

- $R^2$ 只看拟合效果，所以加入变量后不会下降。
- 调整 $R^2$ 会惩罚无用变量，因此加入无关变量时通常会下降。
- 严格来说，如果随机变量碰巧明显改善拟合，调整 $R^2$ 也可能上升。
